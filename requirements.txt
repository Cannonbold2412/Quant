# Dependencies are declared in pyproject.toml and locked in uv.lock.
# This file remains as a convenience entry point for pip users:
#
#   pip install -r requirements.txt       # runtime
#   pip install -e ".[dev]"               # runtime + pytest/ruff
#
# The supported workflow is uv, which is faster and reproducible:
#
#   uv sync --extra dev                   # runtime + dev into .venv
#   uv run pytest                         # run inside .venv
-e .
